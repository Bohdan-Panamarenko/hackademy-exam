import { SignFormProps } from "../Sign"

export default function SignUp(props: SignFormProps) {
    return(
        <div>
            <h1 className="noto font-normal fsz-18 text-left">Sign up</h1>
        </div>
    )
}